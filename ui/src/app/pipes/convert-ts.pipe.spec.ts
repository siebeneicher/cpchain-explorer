import { ConvertTsPipe } from './convert-ts.pipe';

describe('ConvertTsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTsPipe();
    expect(pipe).toBeTruthy();
  });
});
